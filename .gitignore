# 環境変数(シークレットを含む)
.env
.env.*
!.env.example

# Wranglerのローカルシークレットファイル
.dev.vars
.dev.vars.*
!.dev.vars.example

# OpenTofuの状態ファイル・変数ファイル(シークレットを含む)
.terraform
*.tfstate
*.tfstate.backup
*.tfvars
*.auto.tfvars

node_modules

# OS生成ファイル
# macOS
.DS_Store
# Windows
Thumbs.db
desktop.ini
# Linux(KDE Plasma)
.directory

# 共通ディレクトリのエイリアス
apps/api/db
apps/public-api/db
apps/api/lib
apps/public-api/lib
apps/client/lib
apps/admin/lib

# ローカル開発の蓄積データ
apps/email
# Wrangler D1・KV・R2ローカルモードの実データ置き場
.wrangler

# MikroORMのメタデータキャッシュ・コンパイル済み関数出力先
/apps/db/temp
/apps/db/metadata.json
/apps/db/compiled-functions.js

# フロントエンドのビルド成果物
.tanstack
.output
/apps/client/dist
/apps/admin/dist
storybook-static
*.tsbuildinfo

# テスト成果物
# Bun(`bun test --coverage`のデフォルト出力先)
coverage
# Playwright(スクリーンショット・動画・トレースの格納先)
test-results
# Playwright(HTMLレポートの出力先)
playwright-report
# Playwright MCP・Chrome DevTools MCPの出力先
.playwright-mcp

# 生成AI用
.claude/settings.local.json
