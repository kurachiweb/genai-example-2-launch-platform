# 環境変数(シークレットを含む)
.env
.env.*
!.env.example

# Wranglerのローカルシークレットファイル
.dev.vars
.dev.vars.*
!.dev.vars.example

# Terraformの状態ファイル・変数ファイル(シークレットを含む)
.terraform
*.tfstate
*.tfstate.backup
*.tfvars
*.auto.tfvars

node_modules

# OS生成ファイル
# macOS
.DS_Store
# Windows
Thumbs.db
desktop.ini
# Linux(KDE Plasma)
.directory

# 共通ディレクトリのエイリアス
apps/api/db
apps/public-api/db
apps/api/lib
apps/public-api/lib
apps/client/lib
apps/admin/lib

# ローカル開発の蓄積データ
apps/email
# Wrangler D1・KV・R2ローカルモードの実データ置き場
.wrangler

# MikroORMのメタデータキャッシュ出力先
temp
metadata.json

# フロントエンドのビルド成果物
.tanstack
.output
dist
build
storybook-static
*.tsbuildinfo

# テスト成果物
# Bun(`bun test --coverage`のデフォルト出力先)
coverage
# Playwright(スクリーンショット・動画・トレースの格納先)
test-results
# Playwright(HTMLレポートの出力先)
playwright-report

# 生成AI用
.claude/settings.local.json
